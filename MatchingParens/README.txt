Super basic problem to see if you can detect if (, [, or { have their matching ), ], or } in a string.

I implemented this in a fancy way using a map to easily check for the corresponding match but that's overkill.